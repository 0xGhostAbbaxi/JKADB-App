"use client";
import { useState } from "react";
export default function Reports(){const [type,setType]=useState("summary");const [data,setData]=useState<any>(null);const run=()=>fetch(`/api/admin/reports?type=${type}`).then(r=>r.json()).then(setData);return <div className="space-y-6"><div><h1 className="text-2xl font-black">Reports</h1><p className="text-sm text-slate-500">Generate reports from live PostgreSQL data.</p></div><div className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap gap-3"><select value={type} onChange={e=>setType(e.target.value)} className="rounded-xl border px-4 py-3"><option value="summary">Complaint Summary</option><option value="categories">Category Report</option><option value="departments">Department Report</option></select><button onClick={run} className="rounded-xl bg-green-800 px-5 py-3 font-bold text-white">Generate Report</button></div></div>{data&&<pre className="overflow-auto rounded-2xl bg-slate-950 p-5 text-sm text-green-200">{JSON.stringify(data,null,2)}</pre>}</div>}
