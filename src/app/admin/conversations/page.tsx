"use client";
import {useEffect,useState} from "react";import Link from "next/link";
export default function Conversations(){const [d,setD]=useState<any>(null);const [q,setQ]=useState("");const load=()=>fetch(`/api/admin/complaints?search=${encodeURIComponent(q)}&limit=50`,{cache:"no-store"}).then(r=>r.json()).then(setD);useEffect(()=>{load()},[]);return <div className="space-y-6"><div><h1 className="text-2xl font-black">Chat / Conversations</h1><p className="text-sm text-slate-500">Application conversations are managed through complaint threads.</p></div><div className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search tracking number or citizen" className="flex-1 rounded-xl border px-4 py-3"/><button onClick={load} className="rounded-xl bg-green-800 px-5 py-3 font-bold text-white">Search</button></div><div className="grid gap-3">{(d?.complaints||[]).map((c:any)=><Link key={c.id} href={`/admin/complaints/${c.id}`} className="rounded-2xl border bg-white p-5 hover:shadow"><div className="flex justify-between gap-3"><b>{c.trackingNumber}</b><span className="text-xs">{new Date(c.submittedAt).toLocaleString()}</span></div><p className="mt-2 font-semibold">{c.fullName}</p><p className="mt-1 text-sm text-slate-500 line-clamp-2">{c.description}</p></Link>)}</div></div>}
