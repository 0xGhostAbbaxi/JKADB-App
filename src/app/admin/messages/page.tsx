"use client";
import {useEffect,useState} from "react";import Link from "next/link";
export default function Messages(){const [d,setD]=useState<any>(null);const [q,setQ]=useState("");const load=()=>fetch(`/api/admin/messages?search=${encodeURIComponent(q)}`,{cache:"no-store"}).then(r=>r.json()).then(setD);useEffect(()=>{load()},[]);return <div className="space-y-6"><div><h1 className="text-2xl font-black">Messages</h1><p className="text-sm text-slate-500">Search and inspect messages connected to real complaints.</p></div><div className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search message, sender or tracking number" className="flex-1 rounded-xl border px-4 py-3"/><button onClick={load} className="rounded-xl bg-green-800 px-5 py-3 font-bold text-white">Search</button></div><div className="overflow-x-auto rounded-2xl border bg-white"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Message</th><th className="p-3">Sender</th><th className="p-3">Request</th><th className="p-3">Type</th><th className="p-3">Time</th></tr></thead><tbody>{(d?.messages||[]).map((m:any)=><tr key={m.id} className="border-t"><td className="p-3 max-w-xl">{m.content}</td><td className="p-3">{m.senderName||m.citizenName||"—"}</td><td className="p-3"><Link className="text-green-800 font-bold" href={`/admin/complaints/${m.complaintId}`}>{m.trackingNumber}</Link></td><td className="p-3">{m.messageType}</td><td className="p-3">{new Date(m.createdAt).toLocaleString()}</td></tr>)}</tbody></table></div></div>}
